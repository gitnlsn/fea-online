# Rust build output
/target
**/*.rs.bk

# Node
node_modules
.pnpm-store

# Next.js
.next
out
next-env.d.ts

# Generated by scripts/build-wasm.sh -- rebuilt on install and on Vercel, so it
# is never committed. After cloning, run `pnpm build:wasm` (or just `pnpm dev`,
# which runs it first).
/apps/web/wasm/
/apps/web/public/fea_wasm_bg.wasm

# Local env and editor state
.env*.local
.vercel
.DS_Store
*.log
